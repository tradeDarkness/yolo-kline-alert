# 1. 数据集根目录 (使用绝对路径最稳，防止指错)
path: /Users/zhangzc/2026/yolo-kline-alert/data/kline_dataset

# 2. 训练集与验证集 (相对于 path 的路径)
train: train/images
val: train/images   # 偷懒做法：验证集也先用训练集，保证代码能跑通，Demo演示没问题

# 3. 类别名称
names:
  0: ma_cluster     # 强烈建议改成英文！避免 matplotlib 画图时再次报错显示方块